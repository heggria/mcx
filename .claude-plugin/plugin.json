{
  "name": "mcx",
  "description": "Unified semantic catalog for Claude Code: indexes MCP tools AND Claude skills. Lets the model lazy-load both via `mcx search` + `mcx skill show` / `mcx call` instead of dumping every tool schema and every skill description into context at startup. Hybrid BM25 + multilingual embedding (CJK-friendly). Run /mcx-install if missing.",
  "version": "0.2.0",
  "author": {
    "name": "heggria"
  },
  "homepage": "https://github.com/heggria/mcx"
}
